# Pinned environment for the canonical build.
#
# `pip install -r requirements-lock.txt` reproduces the environment that produced
# the artifacts whose md5s are published in src/proforma20q/checksums.json. The
# looser deps in pyproject.toml are enough to SCORE forecasts and run the
# baselines on a modern stack; this lockfile is what a reviewer uses to reproduce
# the data build to float precision (per the paper's "one command rebuilds the
# exact data environment").
#
# Frozen from the canonical build machine (Python 3.12.4, Windows).
# NOTE: the canonical build uses a modern numpy-2 / pandas-2 stack (this is the
# environment that produced the published checksums). Feature-column imputation
# uses scipy.linalg.eigh, whose last-bit results can differ across BLAS/LAPACK
# builds; the scored TARGET columns reproduce to float32 precision regardless.
# Use `proforma20q report-drift` to quantify any per-column vintage/env drift.

numpy==2.3.2
pandas==2.2.3
scipy==1.15.1
scikit-learn==1.6.1
fastparquet==2024.11.0
pyyaml==6.0.2
joblib==1.4.2
tqdm==4.67.1

# Download path only (not needed to score forecasts):
wrds==3.4.0
